#include "main.h"

/**
 * _islower - checks if a input
 * is a lowercase alphabet
 *
 * Return: 0 if false and 1 if correct
 */ 



int _islower(int c)
{


	if ((c <= 'a') || (c >= 'z'))
		return (1);
	else
		return (0);





}
