#include "main.h"

/**
 * _islower - checks if a input
 * is a lowercase alphabet
 *@c: is the characetr to check
 * Return: 0 if false and 1 if correct
 */



int _islower(int c)
{

	if ((c <= 'a') || (c >= 'z'))
		return (0);
	else
		return (1);





}
