#include <stdio.h>

/**
 * main - prints out file name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
