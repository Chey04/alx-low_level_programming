#include "main.h"

/**
 * _isdigit - checks for digit 1 - 9
 *
 * @c: value to be checked
 *
 * Return: 0 if not digit
 */

int _isdigit(int c)
{
	if ((c <= 48) || (c >= 57))
		return (1);
	else if
		return (0);



}
