#include "main.h"

/**
 * print_diagonal - prints diagonal lines
 * according to user input
 *
 * @n: user input
 *
 * Return: 0
 */

void print_diagonal(int n)
{
	int m;

	if (n <= 0)
	{
		_putchar('\n');
	}
	else if
	{
		for (m = 1; m <= n; m++)
		{
			if (m == n)
			{
				_putchar(92);
			}
			else if
			{
				_putchar(32);
			}
		}

	}
}
