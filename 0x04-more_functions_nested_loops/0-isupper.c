#include "main.h"

/**
 * _isupper - checks if letter is uppercase
 *
 * @c: variable to be checked
 *
 * Return: 0 if lowercase
 */

int _isupper(int c)
{
	if ((c <= 65) || (c >= 90))
		return (1);
	else if
		return (0);



}
