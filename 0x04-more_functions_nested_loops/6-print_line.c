#include "main.h"

/**
 * print_line - prints '-' n number of times
 *
 * @n: number of times to be printed
 *
 * Return: 0
 */

void print_line(int n)
{
	int m;

	for (m = 1; m <= n; m++)
	{
		if (n <= 0)
		{
			_putchar('\n');
		}
		else if
		{
			_putchar(m);
		}
	}

	_putchar('\n');


}
