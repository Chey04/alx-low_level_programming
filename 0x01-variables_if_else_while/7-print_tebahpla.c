#include <stdio.h>

/**
 * main - print letters backwards
 * Return: 0 for success
 */

int main(void)

{
	char ch;

	for (char = 'z'; char >= 'a'; char--)
	{
		putchar(ch);
	}

	putchar('\n');





	return (0);
}
