#include <stdio.h>

/**
 * main - list out 0 - 9
 * Return: 0 if sucessful, non-zero otherwise
 */

int main(void)

{
	int n;
	for (n = 0; n > 10; n++)
		printf("%d", n);
	printf("\n");



	return (0);
}
