#include "main.h"

/**
 * string_toupper - changes a string to uppercase
 * Return: the uppercase string
 */

char *string_toupper(char *)
{
	int i;

	while (s[i] != '\0')
	{
		if (s[i] >= 97 && s[i] <= 122
			s[i] -= 32;
			i++;

	}
	return (s);
}
